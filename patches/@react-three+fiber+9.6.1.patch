diff --git a/node_modules/@react-three/fiber/dist/events-583399dd.cjs.prod.js b/node_modules/@react-three/fiber/dist/events-583399dd.cjs.prod.js
index 3376d70..b52ceac 100644
--- a/node_modules/@react-three/fiber/dist/events-583399dd.cjs.prod.js
+++ b/node_modules/@react-three/fiber/dist/events-583399dd.cjs.prod.js
@@ -970,6 +970,32 @@ function createEvents(store) {
 
 const isRenderer = def => !!(def != null && def.render);
 const context = /* @__PURE__ */React__namespace.createContext(null);
+class FiberTimer extends THREE__namespace.Timer {
+  constructor() {
+    super();
+    this.elapsedTime = 0;
+    this.oldTime = 0;
+    this.running = true;
+    this.reset();
+  }
+  start() {
+    this.reset();
+    this.elapsedTime = 0;
+    this.oldTime = 0;
+    this.running = true;
+  }
+  stop() {
+    this.running = false;
+  }
+  getDelta() {
+    if (!this.running) return 0;
+    this.update();
+    const delta = super.getDelta();
+    this.oldTime = this.elapsedTime;
+    this.elapsedTime += delta;
+    return delta;
+  }
+}
 const createStore = (invalidate, advance) => {
   const rootStore = traditional.createWithEqualityFn((set, get) => {
     const position = new THREE__namespace.Vector3();
@@ -1038,7 +1064,7 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE__namespace.Clock(),
+      clock: new FiberTimer(),
       pointer,
       mouse: pointer,
       frameloop: 'always',
diff --git a/node_modules/@react-three/fiber/dist/events-b389eeca.esm.js b/node_modules/@react-three/fiber/dist/events-b389eeca.esm.js
index 97d8295..e116722 100644
--- a/node_modules/@react-three/fiber/dist/events-b389eeca.esm.js
+++ b/node_modules/@react-three/fiber/dist/events-b389eeca.esm.js
@@ -945,6 +945,32 @@ function createEvents(store) {
 
 const isRenderer = def => !!(def != null && def.render);
 const context = /* @__PURE__ */React.createContext(null);
+class FiberTimer extends THREE.Timer {
+  constructor() {
+    super();
+    this.elapsedTime = 0;
+    this.oldTime = 0;
+    this.running = true;
+    this.reset();
+  }
+  start() {
+    this.reset();
+    this.elapsedTime = 0;
+    this.oldTime = 0;
+    this.running = true;
+  }
+  stop() {
+    this.running = false;
+  }
+  getDelta() {
+    if (!this.running) return 0;
+    this.update();
+    const delta = super.getDelta();
+    this.oldTime = this.elapsedTime;
+    this.elapsedTime += delta;
+    return delta;
+  }
+}
 const createStore = (invalidate, advance) => {
   const rootStore = createWithEqualityFn((set, get) => {
     const position = new THREE.Vector3();
@@ -1013,7 +1039,7 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE.Clock(),
+      clock: new FiberTimer(),
       pointer,
       mouse: pointer,
       frameloop: 'always',
diff --git a/node_modules/@react-three/fiber/dist/events-f19bcc32.cjs.dev.js b/node_modules/@react-three/fiber/dist/events-f19bcc32.cjs.dev.js
index 1b04ea6..261b85a 100644
--- a/node_modules/@react-three/fiber/dist/events-f19bcc32.cjs.dev.js
+++ b/node_modules/@react-three/fiber/dist/events-f19bcc32.cjs.dev.js
@@ -970,6 +970,32 @@ function createEvents(store) {
 
 const isRenderer = def => !!(def != null && def.render);
 const context = /* @__PURE__ */React__namespace.createContext(null);
+class FiberTimer extends THREE__namespace.Timer {
+  constructor() {
+    super();
+    this.elapsedTime = 0;
+    this.oldTime = 0;
+    this.running = true;
+    this.reset();
+  }
+  start() {
+    this.reset();
+    this.elapsedTime = 0;
+    this.oldTime = 0;
+    this.running = true;
+  }
+  stop() {
+    this.running = false;
+  }
+  getDelta() {
+    if (!this.running) return 0;
+    this.update();
+    const delta = super.getDelta();
+    this.oldTime = this.elapsedTime;
+    this.elapsedTime += delta;
+    return delta;
+  }
+}
 const createStore = (invalidate, advance) => {
   const rootStore = traditional.createWithEqualityFn((set, get) => {
     const position = new THREE__namespace.Vector3();
@@ -1038,7 +1064,7 @@ const createStore = (invalidate, advance) => {
       linear: false,
       flat: false,
       controls: null,
-      clock: new THREE__namespace.Clock(),
+      clock: new FiberTimer(),
       pointer,
       mouse: pointer,
       frameloop: 'always',
